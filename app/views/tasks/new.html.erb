<%= form_for (@task) do |f| %>
  <p><%= f.label :title %></p>
  <%= f.text_field :title %>
  <p> <%= f.label :details %></p>
  <%= f.text_field :details %>
  <p></i><%= f.submit %></p>
<% end %>

<%= link_to "Back to list", tasks_path %>

 <!-- <i class="fas fa-plus"> -->

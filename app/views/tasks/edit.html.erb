<%= form_for (@task) do |f| %>
  <p><%= f.label :title %></p>
  <%= f.text_field :title %>
  <p> <%= f.label :details %></p>
  <%= f.text_field :details %>
  <%= f.check_box :completed %>
  <p> <i class="fas fa-plus"></i><%= f.submit %></p>
<% end %>

<%= link_to "Back to list", tasks_path %>
